<?xml version="1.0" encoding="utf-8"?>

<Page
    x:Class="URL_Opening_Selector.SettingsPages.LogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:URL_Opening_Selector.SettingsPages"
    xmlns:global="using:URL_Opening_Selector"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:labs="using:CommunityToolkit.Labs.WinUI"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <labs:TokenView Grid.Row="0"
                        SelectionMode="Multiple"
                        x:Name="TokenView1">
            <labs:TokenItem Content="Debug" />
            <labs:TokenItem Content="Info" />
            <labs:TokenItem Content="Warning" />
            <labs:TokenItem Content="Error" />
        </labs:TokenView>
        <ListView Grid.Row="1">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="global:Log">
                    <Border>
                        <StackPanel>
                            <TextBlock Text="{x:Bind Message}" />
                            <TextBlock Text="{x:Bind Time}"
                                       Style="{StaticResource CaptionTextBlockStyle}" />

                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>